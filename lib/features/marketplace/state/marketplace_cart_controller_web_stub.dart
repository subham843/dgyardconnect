import 'package:flutter/foundation.dart';

/// Web stub — marketplace cart is mobile-only (not in web router bundle).
class MarketplaceCartController extends ChangeNotifier {
  @override
  void dispose() {}

  void attachToAuth() {}
}
