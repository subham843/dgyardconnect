import 'package:flutter/material.dart';

/// Navigator key for root-level navigation (e.g. from modals, FCM, deep links).
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
